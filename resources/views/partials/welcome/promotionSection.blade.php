	<!-- Promotion section -->
	<div class="promotion-section">
		<div class="container">
			<div class="row">
				<div class="col-md-9">
					<h2>{{$ready->titre}}</h2>
					<p>{{$ready->sous_titre}}</p>
				</div>
				<div class="col-md-3">
					<div class="promo-btn-area">
						<a href="{{$ready->bouton_url}}" class="site-btn btn-2">{{$ready->bouton_nom}}</a>
					</div>
				</div>
			</div>
		</div>
	</div>
	<!-- Promotion section end-->